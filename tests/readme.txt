These are sample inputs and tests for the different parts of the Bank OCR
Kata. In each directory, there is an answers.txt, which list filesnames of all
tests, with the expected output next to the corresponding filename.
